User-agent: *
Disallow: /

# forma-201: allow HTML crawling so page-level noindex can be read.
# Other projects and non-HTML assets keep the existing crawl restriction.
Allow: /forma-201/$
Allow: /forma-201/*/$
Allow: /forma-201/*.html$

# rieltor: allow demo HTML crawling so its noindex directives can be read.
# Images and other projects keep the existing restrictions.
Allow: /rieltor/$
Allow: /rieltor/*.html$
